function log(message){
    console.log(message);
}
var msg="Hello Word"
log(msg);
// running file tsc main.js auto create main.js file

let temp:number;
let b :boolean;
let arr:number[];
enum color{Red=0, Green=1, Blue=2};
temp = 5;
 
// temp = 'a';
// temp = true;
let bg=color.Blue;
console.log(temp)
